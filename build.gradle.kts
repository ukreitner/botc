// Intentionally minimal: plugin versions are declared in settings.gradle.kts
// so that the pure-JVM :engine module can be built and tested even in
// environments where the Android Gradle Plugin cannot be resolved.
