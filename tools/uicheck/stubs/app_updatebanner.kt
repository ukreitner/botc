@file:Suppress("unused", "PackageDirectoryMismatch", "UNUSED_PARAMETER")

package com.clocktower.grimoire

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

// JVM stand-in for the real UpdateBanner (excluded from this build because
// it drives Android's package installer).
@Composable
fun UpdateBanner(modifier: Modifier = Modifier) {
}
