@file:Suppress("unused", "PackageDirectoryMismatch", "ClassName")

package com.clocktower.grimoire

// JVM stand-in for the AGP-generated BuildConfig (this build has no
// Android Gradle plugin, so nothing generates the real one).
object BuildConfig {
    const val BUILD_SHA: String = "dev"
    const val APPLICATION_ID: String = "com.clocktower.grimoire"
}
