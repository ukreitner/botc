package com.clocktower.engine.rules

import com.clocktower.engine.CharacterRule

/** Bad Moon Rising behaviour. Owned by WP7-BMR; WP0 created the empty file. */
internal val BMR_RULES: List<CharacterRule> = emptyList()
