package com.clocktower.engine.rules

import com.clocktower.engine.CharacterRule

/** Fabled and Loric behaviour. Owned by WP7-FAB; WP0 created the empty file. */
internal val FABLED_RULES: List<CharacterRule> = emptyList()
