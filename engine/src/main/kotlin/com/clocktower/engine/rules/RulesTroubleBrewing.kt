package com.clocktower.engine.rules

import com.clocktower.engine.CharacterRule

/** Trouble Brewing behaviour. Owned by WP7-TB; WP0 created the empty file. */
internal val TB_RULES: List<CharacterRule> = emptyList()
