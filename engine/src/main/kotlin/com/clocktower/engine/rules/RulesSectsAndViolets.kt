package com.clocktower.engine.rules

import com.clocktower.engine.CharacterRule

/** Sects & Violets behaviour. Owned by WP7-SV; WP0 created the empty file. */
internal val SV_RULES: List<CharacterRule> = emptyList()
