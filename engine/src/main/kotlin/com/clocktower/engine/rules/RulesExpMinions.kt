package com.clocktower.engine.rules

import com.clocktower.engine.CharacterRule

/** Experimental Minion behaviour. Owned by WP7-EXP-M; WP0 created the empty file. */
internal val EXP_MINION_RULES: List<CharacterRule> = emptyList()
